<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Categories</title>
    <!-- Include Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.4.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Custom styles for the table */
        .table {
            width: 50%;
            /* Adjust the width as needed */
            margin: 20px auto;
            /* Center the table */
            border: 1px solid #ddd;
            /* Add border */
            border-radius: 5px;
            /* Add border radius */
        }

        .table th,
        .table td {
            padding: 8px;
            border-bottom: 1px solid #ddd;
            text-align: left;
        }

        /* Custom styles for buttons */
        .btn {
            text-decoration: none;
            padding: 6px 12px;
            border: 1px solid transparent;
            border-radius: 4px;
            cursor: pointer;
            font-size: 14px;
            line-height: 1.42857143;
        }

        .btn-primary {
            color: #fff;
            background-color: #337ab7;
            border-color: #2e6da4;
        }

        .btn-danger {
            color: #fff;
            background-color: #d9534f;
            border-color: #d43f3a;
        }

        .btn-sm {
            padding: 5px 10px;
            font-size: 12px;
        }

        .btn:hover,
        .btn:focus {
            color: #333;
            text-decoration: none;
        }
    </style>
</head>

<body>
    <div class="container mt-4">

        <h2>ข้อมูลประเภทสินค้า</h2>
        <a href="/admin/add" class="btn btn-primary">เพิ่มข้อมูลประเภทสินค้า</a>
        <table class="table">
            <thead>
                <tr>
                    <th>ชื่อ</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                <% categories.forEach(category=> { %>
                    <tr>
                        <td>
                            <%= category.name %>
                        </td>
                        <td>
                            <a href="/admin/edit/category/<%= category.id %>" class="btn btn-sm btn-primary">แก้ไข</a>
                            <a href="/admin/delete/category/<%= category.id %>" class="btn btn-sm btn-danger">ลบ</a>
                        </td>
                    </tr>
                    <% }); %>
            </tbody>
        </table>
        <!-- categories.ejs -->
        <!-- categories.ejs -->
        ...
        <% if (currentPage> 1) { %>
            <a href="/admin/categories?page=<%= currentPage - 1 %>" class="btn btn-primary">ก่อนหน้า</a>
            <% } %>
                <% if (hasNextPage) { %>
                    <a href="/admin/categories?page=<%= currentPage + 1 %>" class="btn btn-primary">ถัดไป</a>
                    <% } %>
                        ...


                        <a href="/admin" class="btn btn-primary">ย้อนกลับไปที่หน้า Admin</a>
    </div>


</body>

</html>