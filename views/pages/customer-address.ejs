<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Customer Address</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
    <div class="container">
        <h1 class="mt-5">Customer Address</h1>
        <div class="card mt-3">
            <div class="card-body">
                <p><strong>Name:</strong>
                    <%= customer.FirstName %>
                        <%= customer.LastName %>
                </p>
                <p><strong>Email:</strong>
                    <%= customer.Email %>
                </p>
                <p><strong>Phone Number:</strong>
                    <%= customer.PhoneNumber %>
                </p>
                <p><strong>Address:</strong>
                    <%= customer.AddressLine1 %>, <%= customer.District %>, <%= customer.SubDistrict %>, <%=
                                    customer.Province %>
                                    <%= customer.PostalCode %>
                </p>
                <p><strong>Payment Slip:</strong></p>
                <img src="<%= customer.payment.replace(/\\/g, '/') %>" alt="Product Image"
                    style="max-width: 300px; max-height: 300px;">

                <!-- Check if payment has been made -->
                <% if (paymentMade) { %>
                    <p><strong>สถานะการชำระเงิน:</strong> ชำระเงินเเล้ว</p>
                    <% } else { %>
                        <p><strong>สถานะการชำระเงิน:</strong> ยังไม่ชำระเงิน</p>
                        <form action="/admin/orders/pay/<%= customer.user_id %>" method="POST">
                            <button type="submit" class="btn btn-primary">Mark as Paid</button>
                        </form>
                        <% } %>

            </div>
        </div>
    </div>


</body>

</html>